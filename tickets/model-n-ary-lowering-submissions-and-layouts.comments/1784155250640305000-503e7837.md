---
id: 1784155250640305000-503e7837
by: "codex-root"
at: 1784155250
---
Implemented a shared pure binary lowering graph classifier used by production execution, the n-ary cost model, and the benchmark oracle. It models pair-local reductions, multiply/GEMM/zero graphs, eager broadcast and stride-aware packing copies, and output layouts; selected execution traces assert estimate equality. Added a bounded repeated-signature member-sequence cache that rebuilds step estimates on hits. The 1,001-sample debug production-selector p95 remains below the 175 us ceiling for all four fixtures; the 501-sample wall matrix shows 2.03x current/selected for balanced and 1.10x for broadcast-heavy, with expected planner overhead on the two below-threshold controls. Full root and benchmark tests/clippy pass.
