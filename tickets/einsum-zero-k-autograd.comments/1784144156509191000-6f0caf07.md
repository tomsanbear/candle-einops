---
id: 1784144156509191000-6f0caf07
by: "behavior-tests"
at: 1784144156
---
Closure: RED tests reproduced missing gradients for K=0 and empty B/M/N contractions while forward values, dtype parity, and validation ordering already passed. GREEN adds post-validation graph-preserving zero anchors for both operands and returns before GEMM; the three-size benchmark family records zero hypothetical contraction FLOPs and zero GEMM submissions. Coverage confirms exact zero outputs, present correctly shaped zero gradients for both operands, BF16/integer forward parity where Candle supports it, and CPU plus available-accelerator agreement with the direct construction. Gates passed: full workspace and benchmark tests, locked benchmark compile and registry smoke, benchmark clippy, performance/artifact policy validation, packaged-artifact tests, ticketsplease lint, and exact-base guard. Repository-wide strict clippy remains limited only by documented pre-existing warnings outside this ticket.
