[project]
name = "candle-einops-performance-reporting"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "matplotlib>=3.10,<4",
    "pandas>=2.3,<3",
    "seaborn>=0.13.2,<0.14",
]

[tool.uv]
package = false
