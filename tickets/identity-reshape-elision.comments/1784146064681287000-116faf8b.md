---
id: 1784146064681287000-116faf8b
by: "behavior-tests"
at: 1784146064
---
Closure evidence:

- RED commit `f416838` failed exactly two identity assertions: contiguous reshape returned `TensorId(19)` instead of `TensorId(6)`, and non-contiguous reshape returned `TensorId(22)` instead of `TensorId(9)`. Shape-change and invalid-metadata boundary tests remained green.
- GREEN preserves validation and delegates element-count mismatches and valid shape changes to the existing Candle reshape path; exact dimensions return a shallow clone preserving tensor identity, storage, layout, values, and autograd.
- Ten-sample library/reference ratios: contiguous construct 3.4643x (145.5 ns / 42 ns), contiguous consume 1.6680x (208.5 ns / 125 ns), non-contiguous construct 2.9762x (125 ns / 42 ns), non-contiguous consume 1.00134x (25.622396 ms / 25.5881665 ms).
- Gates passed: full Rust workspace, isolated benchmark workspace and contract, benchmark compile/registry smoke, Python parity workflow and policy, formatting, performance/artifact policy, published-artifact audit, ticket lint, and exact-base scope guard. Strict benchmark clippy passed. Strict root clippy encountered only the pre-existing macro `map_flatten` warning; root clippy passed with the established baseline exceptions.
