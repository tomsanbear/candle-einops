---
id: 1784143389653508000-f4578b81
by: "python-oracle-design"
at: 1784143389
---
TDD evidence: the RED contract failed on unresolved SamplingOrderPolicy/run_synchronized_operation and absent order-policy fields before implementation. GREEN adds deterministic nonuniform binary and broadcast inputs, pairwise-distinct non-square binary outer/GEMM dimensions, a clock-free synchronized Criterion operation seam used by every current registry (including repeat and n-ary), deterministic L/R then R/L paired JSON sampling, and an additive schema-v1 sampling_order_policy with legacy fixed-order default and unchanged fingerprint. Binary scenario ids remain stable but the workload discontinuity is documented. Gates passed: all benchmark tests, focused contract/schema round-trip, fmt, benchmark clippy -D warnings, Criterion bench --no-run, CPU smoke, all 34 registered scenarios with one JSON sample under schema v1/alternating policy, Metal all-target compile, and root workspace/all-targets/all-features tests. No timing threshold was added.
