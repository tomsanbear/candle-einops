---
id: 1784147539009341000-bb6532b9
by: "behavior-tests"
at: 1784147539
---
Closure evidence:

- RED commit `049b930` failed to compile because the committed decision-table contract imported the deliberately absent `permute_compose_layout_spike` module.
- The spike-only classifier distinguishes identity elision, existing public reshape views, locally collapsible layouts, safe whole-group public-operation reorderings, and exact copy fallbacks. Tests cover values, storage, strides, offsets, autograd, singleton/zero extents, invalid metadata, checked overflow, non-collapsible order, bounded-search fallback, and genuine non-contiguous sources in both eligible recovery and ineligible fallback cases. No production runtime or macro fusion was added.
- Conditional GO: `c (a b)` construction measured 8.4903125 ms current versus 6.896 us prototype (1231.1938x); `n (h w) c` measured 8.505646 ms versus 9.9795 us (852.3118x). Immediate contiguous consumption was neutral at 1.00476x and 0.99711x, proving the candidate can defer rather than eliminate a required downstream copy. The implementation ticket now contains exact public-API eligibility, flatten-order, identity, backend, and byte-for-byte fallback rules.
- Gates passed before rebase: full Rust workspace; complete isolated benchmark workspace and registry contracts; focused spike tests; strict isolated clippy; root clippy with established baseline exceptions (strict root remains blocked only by the pre-existing macro `map_flatten` warning); root and benchmark formatting; CPU benchmark compile and 20-sample measurement; Metal feature benchmark compile; Python parity workflow and policy; performance and artifact policies; and the full published-artifact audit. After rebasing onto integration `5e0e320`, the complete benchmark workspace, focused six-test spike suite, registry contract, strict isolated clippy, formatting, diff check, supported wrapper smoke, targeted four-scenario registry run, ticketsplease lint, and exact-base guard all passed while preserving the integrated diagonal registries.
- Independent review approved the classifier, public plan, correctness/materialization evidence, benchmark fairness, registry preservation, decision, conditional ticket refinement, and spike-only scope. Follow-up review reconfirmed approval after direct overflow tests and durable reproduction metadata were added.
